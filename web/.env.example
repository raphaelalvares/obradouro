# Painel web CRIA — variáveis de ambiente (Vite expõe só as prefixadas com VITE_).
# Copie para .env.local e preencha. NUNCA versionar segredos reais.

# API Python (backend FastAPI). É o ÚNICO backend que o front conhece: auth (login/sessão/refresh)
# vai por ela em cookie httpOnly (BFF) e todo dado também. O front não fala direto com o Supabase.
VITE_API_BASE_URL=http://localhost:8000
