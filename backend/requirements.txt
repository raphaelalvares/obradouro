# Runtime do backend CRIA (fonte de verdade das deps para o Docker).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
sqlalchemy[asyncio]>=2.0.36,<3.0
asyncpg>=0.30,<1.0
supabase==2.30.1  # pin exato (verificado) p/ builds Docker reprodutíveis
pyjwt[crypto]>=2.9,<3.0  # validação do JWT do Supabase (assimétrico, via JWKS)
email-validator>=2.0  # pydantic EmailStr (validação de email no convite)
python-multipart>=0.0.9  # UploadFile/multipart (import do template de checklist)
openpyxl>=3.1,<4.0  # parse do template .xlsx de import (Fase 3)
