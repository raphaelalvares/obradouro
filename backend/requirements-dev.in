# Dev/test deps do backend CRIA — FONTE (editável à mão).
# requirements-dev.txt é GERADO (pinado + hashes) — ver o cabeçalho de requirements.in p/ recompilar.
-r requirements.in
ruff>=0.8
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
